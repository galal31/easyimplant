# تعليمات العمل على المشروع

قبل تحليل المشروع أو إجراء أي تعديل، اقرأ الملف التالي كاملًا لأنه يحتوي على السياق المتجدد للنظام والقرارات التي تم فهمها:

`PROJECT_CONTEXT.md`

عند اكتشاف سلوك مهم جديد أو تنفيذ تغيير يؤثر في طريقة عمل النظام، حدّث ملف السياق باختصار وبدون تسجيل كلمات مرور أو مفاتيح سرية أو بيانات حساسة.


## إدارة Git والـ commits

### التواصل مع GitHub والـ remotes

لا تتواصل مع GitHub أو أي remote تلقائيًا. لا تنفذ `push` أو `pull` أو `fetch` أو `clone` أو أي استدعاء لـ GitHub API إلا إذا طلب المستخدم ذلك صراحة في الطلب الحالي. فحوصات Git المحلية وقراءة إعدادات الـ remote مسموحة عند الحاجة، لكن لا تُجرِ أي اتصال شبكي أو مزامنة خارجية من تلقاء نفسك.

في بداية كل محادثة أو طلب جديد داخل هذا المشروع، وقبل تعديل الملفات:

1. افحص `git status --short --branch` لمعرفة الفرع وأي تغييرات غير محفوظة.
2. افحص آخر خمسة commits باستخدام `git log -5 --oneline --decorate`، وتحقق من الـ upstream والريموت عند الحاجة.
3. إذا كانت الشجرة نظيفة والطلب الجديد مستقلًا، اعتبره commit جديدًا منفصلًا.
4. إذا وجدت تغييرات غير محفوظة، افحص الـ diff أولًا وحدد هل هي امتداد لنفس المهمة أم عمل سابق غير مكتمل. لا تخلط مهمتين مستقلتين في commit واحد ولا تتخلص من تعديلات المستخدم.

اجعل كل ميزة أو إصلاح منطقي في commit مستقل برسالة واضحة. بعد اكتمال التعديل ونجاح التحقق المناسب، أنشئ commit محليًا برسالة واضحة. لا ترفع commit إلى `origin/main` أو أي remote إلا إذا طلب المستخدم ذلك صراحة.

قبل كل commit، وقبل أي push طلبه المستخدم صراحة:

- راجع الملفات المجهزة وإجمالي التغييرات.
- شغّل الاختبارات المناسبة للمهمة.
- تأكد أن ملفات الإعداد الفعلية والأسرار وبيانات الرفع والنسخ الاحتياطية لا تدخل Git.
- لا تستخدم `--amend` ولا تعيد كتابة التاريخ المنشور إلا بطلب صريح من المستخدم.
